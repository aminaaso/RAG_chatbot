# 🤖 AI Season RAG Chatbot

A Retrieval-Augmented Generation (RAG) chatbot built using the AI Season Bootcamp documents as its knowledge base.

This project explores how a Large Language Model (LLM) can answer questions more accurately when it is provided with relevant information retrieved from external documents.

Instead of relying only on the LLM's existing knowledge, this chatbot first searches through the AI Season documents, finds the most relevant information, and then uses that context to generate a grounded response.

---

## 📌 About the Project

Large Language Models are powerful, but they have limitations:

- They may not know private or newly updated information.
- They can sometimes generate incorrect answers.
- They do not automatically have access to custom documents.

To solve this problem, I built a **RAG pipeline** where:

1. Documents are loaded and processed.
2. Text is divided into smaller chunks.
3. Chunks are converted into embeddings.
4. The embeddings are stored in a FAISS vector database.
5. Relevant chunks are retrieved based on the user's question.
6. A Groq-powered LLM generates the final answer using the retrieved context.

---

# 🚀 What This Project Does

This chatbot compares different RAG approaches by testing:

## Chunking Strategies

### 1. Recursive Chunking
Splits documents while trying to maintain the meaning and structure of the text.

### 2. Fixed Chunking
Splits documents into fixed-size text segments.

### 3. Sentence Chunking
Splits documents sentence by sentence to preserve smaller pieces of information.


## Retrieval Techniques

### 1. Similarity Search
Finds document chunks that are most similar to the user's query.

### 2. Maximum Marginal Relevance (MMR)
Retrieves relevant information while reducing repeated content.

---

The system compares all combinations:

```
3 Chunking Methods × 2 Retrieval Methods

= 6 Different RAG Outputs
```

For every question, the chatbot displays the answer generated by each approach.

---

# 🏗️ Project Workflow

```
AI Season PDF Documents

        ↓

Document Loading

        ↓

Text Chunking

        ↓

Embedding Generation

        ↓

FAISS Vector Database

        ↓

Information Retrieval

        ↓

Groq LLM

        ↓

Final Answer
```

---

# 🛠️ Technologies Used

- Python
- LangChain
- FAISS Vector Database
- HuggingFace Sentence Transformers
- Groq LLM API
- PyPDF Loader
- dotenv

---

# 📂 Project Structure

```
AI-SEASON-RAG/

│
├── app.py                 # Main RAG pipeline
├── chunking.py            # Different chunking strategies
├── retrievers.py          # Retrieval techniques
├── embeddings.py          # Embedding model setup
├── llm.py                 # LLM answer generation
│
├── data/                  # AI Season PDF documents
│
├── requirements.txt       # Required Python packages
├── README.md
└── .gitignore
```

---

# ⚙️ How to Run the Project

## 1. Clone the repository

```bash
git clone <repository-link>
```

Move into the project folder:

```bash
cd AI-SEASON-RAG
```

---

## 2. Create a virtual environment

```bash
python -m venv venv
```

Activate it:

### Windows:

```bash
venv\Scripts\activate
```

---

## 3. Install dependencies

```bash
pip install -r requirements.txt
```

---

## 4. Add your API key

Create a `.env` file in the project folder:

```
GROQ_API_KEY=your_api_key_here
```

---

## 5. Add documents

Place the AI Season Bootcamp PDF files inside:

```
data/
```

---

## 6. Run the chatbot

```bash
python app.py
```

Ask a question:

Example:

```
What is Retrieval Augmented Generation?
```

The system will generate answers using different chunking and retrieval combinations.

---

# 📊 Example Output

```
==================================================

Recursive Chunking + Similarity Retrieval

==================================================

RAG is a technique that combines retrieval
with language generation...


==================================================

Sentence Chunking + MMR Retrieval

==================================================

RAG improves LLM responses by providing
relevant external knowledge...
```

---

# 💡 Key Learnings

Through this project, I explored:

- How RAG systems work internally
- Why document chunking affects retrieval quality
- How embeddings represent text as vectors
- How vector databases enable semantic search
- The difference between similarity search and MMR retrieval
- How LLMs can generate better answers when provided with relevant context

---

# 🔮 Future Improvements

Some possible improvements:

- Add a Streamlit web interface
- Add conversation memory
- Add document upload functionality
- Add answer citations with source pages
- Deploy the chatbot online

---

# 👩‍💻 Author

**Amina Sohail**  
BS Artificial Intelligence Student
